# Convenciones de código

- Al declarar los tipos de las props de un componente, utiliza `interface` en vez de `type`.
- Cuando se edita un archivo, ya sea nuevo o existente, al finalizar los cambios se debe formatear el mismo utilizando el comando `pnpm exec prettier ./<PATH_AL_ARCHIVO> --write`.
